{
  "name": "pixel-agents-kimi-standalone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:ui\"",
    "dev:server": "tsx watch server/index.ts",
    "dev:ui": "npm --prefix webview-ui run dev",
    "test": "bun test",
    "typecheck": "tsc --noEmit && npm --prefix webview-ui exec -- tsc -b",
    "test:typecheck": "npm run typecheck",
    "lint": "npm --prefix webview-ui run lint",
    "check": "npm run test && npm run typecheck && npm run lint && npm run build",
    "build": "npm run build:server && npm run build:ui",
    "build:server": "esbuild server/index.ts --bundle --platform=node --format=esm --outfile=dist/server.js --external:chokidar --external:ws --external:express --external:pngjs",
    "build:ui": "npm --prefix webview-ui run build",
    "start": "node dist/server.js",
    "import-tileset": "tsx scripts/import-tileset.ts",
    "extract-furniture": "tsx scripts/extract-furniture.ts"
  },
  "dependencies": {
    "chokidar": "^4.0.0",
    "express": "^5.0.0",
    "pngjs": "^7.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "@types/pngjs": "^6.0.5",
    "@types/ws": "^8.5.0",
    "concurrently": "^9.0.0",
    "esbuild": "^0.27.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0"
  }
}
